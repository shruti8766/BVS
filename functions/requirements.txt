# Firebase Cloud Functions (Gen 2)
firebase-functions>=0.5.0
firebase-admin>=6.0.0
functions-framework>=3.3.0

# Flask and web framework (deprecated for Gen 2, but kept for internal routing logic)
flask==3.0.0
flask-cors

# Authentication & Security
pyjwt
bcrypt

# API clients & External services
requests
twilio

# Translation & Language (optional, lazy loaded)
deep-translator>=1.11.4
langdetect>=1.0.9

# Utilities
python-dotenv
werkzeug>=3.0.0
